export interface Extraction{
    id_extraction?: number,
    user_name: string,
    creation_date: Date,
    isAdmin: Boolean,
    company_number: string,
    last_update?: Date,
    priority: number
}

